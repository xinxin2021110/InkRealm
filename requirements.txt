# 小说角色复刻系统依赖

# 核心依赖
openai>=1.0.0
pydantic>=2.0.0

# 可选依赖（用于增强功能）
# numpy>=1.24.0  # 用于向量计算（如果需要更复杂的embedding）
# jieba>=0.42.1  # 用于中文分词（如果需要精确的关键词提取）

# 开发依赖
# pytest>=7.0.0  # 测试框架
# black>=23.0.0  # 代码格式化
# flake8>=6.0.0  # 代码检查
